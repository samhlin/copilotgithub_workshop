# 變更紀錄

本檔案記錄待辦清單 App 的功能變動。

## [0.2.0] - Step 2

### 新增
- **深色模式切換**：標題列右側新增切換按鈕，可在淺色與深色之間切換。
- **記住主題偏好**：使用者的選擇會存進 `localStorage`，重新整理後仍維持原設定。
- **跟隨系統設定**：使用者若從未手動切換過，會自動採用作業系統的深淺色偏好（`prefers-color-scheme`）。
- **篩選功能**：新增「全部」、「未完成」與「已完成」三個篩選按鈕。
- **情境化空狀態文字**：依目前篩選條件顯示對應的空清單提示。

### 變更
- 色彩全面改用 CSS 變數，方便切換淺色與深色主題。
- 「未完成：N 項」的統計不受篩選條件影響，永遠反映整體清單數量。

## [0.1.0] - Step 1

### 新增
- 建立待辦清單 App 的基本結構（`index.html`、`styles.css`、`app.js`）。
- 新增待辦事項。
- 勾選標記完成或取消完成。
- 刪除待辦事項。
- 顯示未完成項目數量。
- 將資料存入 `localStorage`，重新整理後資料仍會保留。
- 置中版面、卡片式設計與基本 RWD。

### 變更
- 建立純 HTML、CSS 與原生 JavaScript 的離線使用方式，不依賴外部框架或套件。
